Todo :

- Snippets
    1. Possibility of adding snippets
    2. Possibility of editing snippets
    3. Possibility of coping snippets
    4. Possibility of searching snippets
    5. Better syntax highlighting for snippet
- File explorer
    1. Add filter
    2. Add search
- CLI tool
    1. Link the cli and the web (with a `serve` command)
    2. Add `stats`, `search`, `show` commands to the cli
    3. Redo the cli tool so it works
- AI assistant
    1. Add possibility of more than one discussions with IA assistant
    2. Add possibility to save chat with assistant between reloads
    3. Add possibility to remove chats
    4. Support streaming response
    5. Possibility to send resources (file, project diskhub, axone, links)
    6. Show shared resources
- Ignoring file
    1. Upgrade the filter for .ignore
    2. Add support for Git ignore
- Other
    1. Link the change of theme
    2. Add links to Axone
    3. Add a proper LICENSE file
    4. Better support for comments and count of LOC
- Wiki
    1. Take the files from .diskhub/wiki
    2. Support a markdown format with some additions
    3. Support basic layout (paragraph, headers, lists, etc)
    4. Support extended format (links between pages, extract from files)
    5. Possibility to select which lines to show (extract of file)
    6. Can easily import other pages inside (If i need to separate one thing to import it in two or more pages)
    7. Have some information about pages on the hover
    8. On hover of extract of file, get the comments associated with extract if existing
    9. Syntax coloration, number of line, name of file (if existing) in corner (as well of language)
    10. WYSIWYG